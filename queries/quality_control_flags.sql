SELECT 
    id,
    packet_id,
    canvasser_id,
    status,
    created_at,
    updated_at,
    trigger_id,
    triggered_by_canvasser_id,
    completed_at,
    triggered_by_shift_id
FROM onearizona.quality_control_flags